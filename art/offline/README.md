# Offline art workspace

This directory is intentionally excluded from Git, Godot imports and playable exports except for this marker and README.

Local crash-dummy SVG masters, raster chunks, labelled atlases and assembled review sheets live under `crash_dummy_v01/`. The public repository contains only their semantic catalog, schema, packer and documentation until individual media files are explicitly promoted.
